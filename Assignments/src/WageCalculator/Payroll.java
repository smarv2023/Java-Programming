package WageCalculator;
// Create an interface called Payroll that requires the creation of a function called generatePayroll();  3pts
// Define the structure of a class
public interface Payroll {
	// Interface give rules to the child to provide own generatePayroll()
	void generatePayroll();
}
